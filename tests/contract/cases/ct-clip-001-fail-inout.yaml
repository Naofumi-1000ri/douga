id: CT-CLIP-001
kind: fail
description: out_point_ms must be greater than in_point_ms
request:
  method: POST
  path: /api/ai/v1/projects/{P}/clips
  headers:
    Idempotency-Key: test-clip-001
    If-Match: etag-1
  body:
    options: { validate_only: true, return_diff: false }
    clip:
      type: video
      layer_id: "{L1}"
      asset_id: "{A}"
      start_ms: 0
      duration_ms: 3000
      in_point_ms: 2000
      out_point_ms: 1000
      transform:
        position: { x: 0, y: 0 }
        scale: { x: 1, y: 1 }
        rotation: 0
        opacity: 1
        anchor: { x: 0.5, y: 0.5 }
context:
  fixture: base.yaml
expect:
  status: 400
  error_code: INVALID_TIME_RANGE
  suggested_fix: "Set out_point_ms > in_point_ms"

id: CT-CLIP-000
kind: pass
description: Add video clip with valid timing and transform
request:
  method: POST
  path: /api/ai/v1/projects/{P}/clips
  headers:
    Idempotency-Key: test-clip-000
    If-Match: etag-1
  body:
    options: { validate_only: true, return_diff: false }
    clip:
      type: video
      layer_id: "{L1}"
      asset_id: "{A}"
      start_ms: 5000
      duration_ms: 3000
      in_point_ms: 0
      out_point_ms: 3000
      transform:
        position: { x: 0, y: 0 }
        scale: { x: 1, y: 1 }
        rotation: 0
        opacity: 1
        anchor: { x: 0.5, y: 0.5 }
context:
  fixture: base.yaml
expect:
  status: 200
  error_code: null
